<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Brawl Academy - Portal de Aprendizagem</title>
  <link href="https://fonts.googleapis.com/css2?family=Luckiest+Guy&family=Nunito:wght@700;900&display=swap" rel="stylesheet">
  <style>
    :root {
      --bg-gradient: linear-gradient(135deg, #1b0a38 0%, #32125f 50%, #110526 100%);
      --brawl-yellow: #fcd015;
      --brawl-yellow-dark: #c69d00;
      --brawl-blue: #00beff;
      --brawl-blue-dark: #007cb3;
      --brawl-green: #2cd938;
      --brawl-purple: #9b27b0;
      --brawl-card-bg: rgba(22, 11, 46, 0.85);
    }

    * {
      box-sizing: border-box;
      user-select: none;
    }

    body {
      margin: 0;
      padding: 0;
      font-family: 'Nunito', sans-serif;
      background: var(--bg-gradient);
      color: #fff;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      overflow-x: hidden;
    }

    /* BARRA SUPERIOR (HEADER) */
    .top-bar {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 12px 24px;
      background: rgba(0, 0, 0, 0.4);
      border-bottom: 4px solid #000;
      backdrop-filter: blur(8px);
    }

    .user-info {
      display: flex;
      align-items: center;
      gap: 12px;
    }

    .avatar-frame {
      width: 55px;
      height: 55px;
      background: #ffaa00;
      border: 3px solid #000;
      border-radius: 12px;
      display: flex;
      justify-content: center;
      align-items: center;
      font-size: 2rem;
      box-shadow: 0 4px 0 #805500;
      position: relative;
    }

    .user-details h3 {
      margin: 0;
      font-family: 'Luckiest Guy', cursive;
      font-size: 1.2rem;
      letter-spacing: 1px;
      color: #fff;
      text-shadow: 2px 2px 0 #000;
    }

    .currencies {
      display: flex;
      gap: 16px;
    }

    .badge {
      background: #000;
      border: 2px solid #fff;
      border-radius: 20px;
      padding: 4px 14px;
      display: flex;
      align-items: center;
      gap: 6px;
      font-family: 'Luckiest Guy', cursive;
      font-size: 1rem;
      box-shadow: 0 4px 0 rgba(0,0,0,0.5);
    }

    .badge.trophies { color: #ffca28; border-color: #ffca28; }
    .badge.coins { color: #ffd54f; border-color: #f57f17; }
    .badge.gems { color: #00e676; border-color: #00b0ff; }

    /* LAYOUT PRINCIPAL */
    .portal-container {
      display: grid;
      grid-template-columns: 320px 1fr;
      gap: 24px;
      padding: 24px;
      max-width: 1300px;
      margin: 0 auto;
      width: 100%;
      flex-grow: 1;
    }

    /* PAINEL DO PERSONAGEM (ESQUERDA) */
    .character-panel {
      background: var(--brawl-card-bg);
      border: 4px solid #000;
      border-radius: 24px;
      padding: 20px;
      display: flex;
      flex-direction: column;
      align-items: center;
      text-align: center;
      box-shadow: 0 8px 0 #000;
      position: relative;
    }

    .brawler-preview {
      width: 180px;
      height: 180px;
      background: radial-gradient(circle, rgba(0,190,255,0.4) 0%, rgba(0,0,0,0) 70%);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 7rem;
      margin: 15px 0;
      filter: drop-shadow(0 10px 10px rgba(0,0,0,0.5));
      animation: float 3s ease-in-out infinite;
    }

    @keyframes float {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-10px); }
    }

    .brawler-name {
      font-family: 'Luckiest Guy', cursive;
      font-size: 1.8rem;
      color: #fff;
      text-shadow: 3px 3px 0 #000;
      margin: 0;
    }

    .brawler-skin-tag {
      background: #e91e63;
      font-size: 0.8rem;
      font-weight: 900;
      padding: 4px 12px;
      border-radius: 10px;
      border: 2px solid #000;
      text-transform: uppercase;
      margin-top: 5px;
    }

    .btn-shop {
      width: 100%;
      margin-top: auto;
      background: linear-gradient(180deg, var(--brawl-yellow) 0%, var(--brawl-yellow-dark) 100%);
      border: 3px solid #000;
      border-radius: 16px;
      padding: 12px;
      font-family: 'Luckiest Guy', cursive;
      font-size: 1.3rem;
      color: #000;
      cursor: pointer;
      box-shadow: 0 6px 0 #725600;
      transition: transform 0.1s, box-shadow 0.1s;
    }

    .btn-shop:active {
      transform: translateY(4px);
      box-shadow: 0 2px 0 #725600;
    }

    /* SELEÇÃO DE ATIVIDADES / MODOS DE JOGO */
    .modes-panel {
      display: flex;
      flex-direction: column;
      gap: 20px;
    }

    .section-title {
      font-family: 'Luckiest Guy', cursive;
      font-size: 2rem;
      color: #fff;
      text-shadow: 3px 3px 0 #000;
      margin: 0;
      letter-spacing: 1px;
    }

    .cards-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 20px;
    }

    .mode-card {
      background: var(--brawl-card-bg);
      border: 4px solid #000;
      border-radius: 20px;
      padding: 18px;
      display: flex;
      flex-direction: column;
      position: relative;
      overflow: hidden;
      cursor: pointer;
      box-shadow: 0 8px 0 #000;
      transition: transform 0.2s;
    }

    .mode-card:hover {
      transform: translateY(-5px);
    }

    .mode-card.pt { border-color: #ff3d00; }
    .mode-card.math { border-color: #00e676; }
    .mode-card.en { border-color: #00b0ff; }

    .card-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 12px;
    }

    .mode-icon {
      font-size: 2.5rem;
    }

    .mode-title {
      font-family: 'Luckiest Guy', cursive;
      font-size: 1.4rem;
      text-shadow: 2px 2px 0 #000;
      margin: 5px 0;
    }

    .mode-desc {
      font-size: 0.9rem;
      color: #ccc;
      margin-bottom: 15px;
    }

    .btn-play {
      background: linear-gradient(180deg, #2cd938 0%, #158320 100%);
      border: 3px solid #000;
      border-radius: 12px;
      padding: 10px;
      font-family: 'Luckiest Guy', cursive;
      font-size: 1.2rem;
      color: #fff;
      text-shadow: 2px 2px 0 #000;
      text-align: center;
      box-shadow: 0 4px 0 #0a4310;
      margin-top: auto;
    }

    /* MODAL DE LOJA / FIGURINHAS */
    .modal-overlay {
      position: fixed;
      top: 0; left: 0; right: 0; bottom: 0;
      background: rgba(0, 0, 0, 0.85);
      display: none;
      justify-content: center;
      align-items: center;
      z-index: 100;
      padding: 20px;
    }

    .modal-content {
      background: #251242;
      border: 5px solid #000;
      border-radius: 24px;
      width: 100%;
      max-width: 600px;
      padding: 24px;
      box-shadow: 0 10px 0 #000;
      position: relative;
    }

    .close-btn {
      position: absolute;
      top: -15px;
      right: -15px;
      background: #ff1744;
      border: 3px solid #000;
      color: #fff;
      width: 40px;
      height: 40px;
      border-radius: 50%;
      font-family: 'Luckiest Guy', cursive;
      font-size: 1.5rem;
      cursor: pointer;
      box-shadow: 0 4px 0 #800b21;
    }

    .stickers-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 15px;
      margin-top: 20px;
    }

    .sticker-item {
      background: rgba(0,0,0,0.4);
      border: 3px solid #000;
      border-radius: 16px;
      padding: 12px;
      text-align: center;
      cursor: pointer;
      position: relative;
    }

    .sticker-item.selected {
      border-color: var(--brawl-yellow);
      background: rgba(252, 208, 21, 0.2);
    }

    .sticker-emoji {
      font-size: 3rem;
    }

    .sticker-name {
      font-size: 0.85rem;
      font-weight: 900;
      margin-top: 5px;
    }

    .sticker-price {
      background: #000;
      border-radius: 10px;
      padding: 2px 6px;
      font-family: 'Luckiest Guy', cursive;
      color: #ffd54f;
      font-size: 0.8rem;
      margin-top: 5px;
      display: inline-block;
    }
  </style>
</head>
<body>

  <!-- BARRA SUPERIOR -->
  <header class="top-bar">
    <div class="user-info">
      <div class="avatar-frame" id="topAvatar">🦁</div>
      <div class="user-details">
        <h3 id="userName">SUPER LEITOR</h3>
      </div>
    </div>
    
    <div class="currencies">
      <div class="badge trophies">🏆 <span id="trophyCount">350</span></div>
      <div class="badge coins">🪙 <span id="coinCount">120</span></div>
      <div class="badge gems">💎 <span id="gemCount">15</span></div>
    </div>
  </header>

  <!-- CONTAINER PRINCIPAL -->
  <div class="portal-container">
    
    <!-- PERFIL DO PERSONAGEM -->
    <aside class="character-panel">
      <h2 class="brawler-name" id="displayBrawlerName">LÉO LEÃO</h2>
      <div class="brawler-skin-tag" id="displaySkinTag">EDIÇÃO BÁSICA</div>
      
      <div class="brawler-preview" id="mainBrawlerIcon">🦁</div>

      <button class="btn-shop" onclick="openShop()">🖼️ ÁLBUM & LOJA</button>
    </aside>

    <!-- ÁREA DE EVENTOS / MODOS DE JOGO -->
    <main class="modes-panel">
      <h2 class="section-title">ARENAS DE APRENDIZADO</h2>
      
      <div class="cards-grid">
        
        <!-- CARD PORTUGUÊS -->
        <div class="mode-card pt" onclick="startActivity('Leitura')">
          <div class="card-header">
            <span class="mode-icon">📖</span>
            <div class="badge trophies">+10 🏆</div>
          </div>
          <h3 class="mode-title" style="color: #ff5722;">LEITURA & LETRAS</h3>
          <p class="mode-desc">Aprenda a ler sílabas, juntar palavras e formar frases!</p>
          <div class="btn-play">JOGAR!</div>
        </div>

        <!-- CARD MATEMÁTICA -->
        <div class="mode-card math" onclick="startActivity('Matemática')">
          <div class="card-header">
            <span class="mode-icon">🔢</span>
            <div class="badge trophies">+10 🏆</div>
          </div>
          <h3 class="mode-title" style="color: #00e676;">DESAFIO DA SOMA</h3>
          <p class="mode-desc">Resolva continas divertidas e ganhe muitas moedas!</p>
          <div class="btn-play">JOGAR!</div>
        </div>

        <!-- CARD INGLÊS -->
        <div class="mode-card en" onclick="startActivity('Inglês')">
          <div class="card-header">
            <span class="mode-icon">🚀</span>
            <div class="badge trophies">+10 🏆</div>
          </div>
          <h3 class="mode-title" style="color: #00b0ff;">ENGLISH QUEST</h3>
          <p class="mode-desc">Descubra novas palavras em inglês com áudios e figuras!</p>
          <div class="btn-play">JOGAR!</div>
        </div>

      </div>
    </main>
  </div>

  <!-- MODAL DE LOJA / FIGURINHAS -->
  <div class="modal-overlay" id="shopModal">
    <div class="modal-content">
      <button class="close-btn" onclick="closeShop()">X</button>
      <h2 class="section-title" style="text-align: center;">ÁLBUM DE FIGURINHAS</h2>
      <p style="text-align: center; color: #ccc; font-size: 0.9rem;">Escolha sua figurinha/skin favorita para exibir no portal!</p>
      
      <div class="stickers-grid">
        
        <div class="sticker-item selected" onclick="selectSticker('🦁', 'Léo Leão', 'BÁSICO', 0)">
          <div class="sticker-emoji">🦁</div>
          <div class="sticker-name">Léo Clássico</div>
          <div class="sticker-price">USANDO</div>
        </div>

        <div class="sticker-item" onclick="selectSticker('⚽', 'Léo Craque', 'FUTEBOL', 50)">
          <div class="sticker-emoji">⚽</div>
          <div class="sticker-name">Léo Craque</div>
          <div class="sticker-price">🪙 50</div>
        </div>

        <div class="sticker-item" onclick="selectSticker('🤖', 'Calculatron', 'ROBÔ', 100)">
          <div class="sticker-emoji">🤖</div>
          <div class="sticker-name">Calculatron</div>
          <div class="sticker-price">🪙 100</div>
        </div>

        <div class="sticker-item" onclick="selectSticker('🦄', 'UniEspecial', 'MÁGICO', 150)">
          <div class="sticker-emoji">🦄</div>
          <div class="sticker-name">UniEspecial</div>
          <div class="sticker-price">🪙 150</div>
        </div>

        <div class="sticker-item" onclick="selectSticker('👑', 'Léo Rei', 'LENDÁRIO', 300)">
          <div class="sticker-emoji">👑</div>
          <div class="sticker-name">Léo Rei</div>
          <div class="sticker-price">🪙 300</div>
        </div>

      </div>
    </div>
  </div>

  <script>
    // Estado simples do jogador
    let playerData = {
      coins: 120,
      trophies: 350,
      activeEmoji: '🦁',
      activeName: 'LÉO LEÃO',
      activeTag: 'EDIÇÃO BÁSICA'
    };

    function openShop() {
      document.getElementById('shopModal').style.display = 'flex';
    }

    function closeShop() {
      document.getElementById('shopModal').style.display = 'none';
    }

    function selectSticker(emoji, name, tag, price) {
      if (price > playerData.coins && price > 0) {
        alert("Moedas insuficientes! Responda mais tarefas para ganhar moedas. 🪙");
        return;
      }

      playerData.activeEmoji = emoji;
      playerData.activeName = name.toUpperCase();
      playerData.activeTag = tag;

      // Atualiza interface
      document.getElementById('mainBrawlerIcon').innerText = emoji;
      document.getElementById('topAvatar').innerText = emoji;
      document.getElementById('displayBrawlerName').innerText = playerData.activeName;
      document.getElementById('displaySkinTag').innerText = playerData.activeTag;

      closeShop();
    }

    function startActivity(modo) {
      // Exemplo de ganho ao simular jogada
      playerData.trophies += 10;
      playerData.coins += 15;

      document.getElementById('trophyCount').innerText = playerData.trophies;
      document.getElementById('coinCount').innerText = playerData.coins;

      alert(`Acessando a Arena de ${modo}!\n\nVocê ganhou +10 Troféus 🏆 e +15 Moedas 🪙!`);
    }
  </script>
</body>
</html>
